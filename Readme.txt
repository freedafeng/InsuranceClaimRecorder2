Backend is in the top folder ***Backend folder.

To run the backend,
pip install -r requirements.txt
uvicorn main:app --host 0.0.0.0 --port 8000

The uploaded video and snapshorts of the video will be in the uploads folder
under the ***Backend folder. 
